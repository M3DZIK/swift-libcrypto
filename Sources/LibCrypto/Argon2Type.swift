/// Enum that represents the type of argon2 (i, d, id)
public enum Argon2Type: Int {
    /// `Argon2i` type
    case i = 0
    /// `Argon2d` type
    case d = 1
    /// `Argon2id` type
    case id = 2
}
